{%
  set classes = [
  'field',
  'field--name-' ~ field_name|clean_class,
  'gallery-item',
]
%}

{% for item in items %}
  <div{{ attributes.addClass(classes) }} data-aos="zoom-in-up">{{ item.content }}</div>
{% endfor %}
